# MyNote Server 配置

# 监听端口
PORT=8080

# 数据目录（存放 mynote.db）
DATA_DIR=./data

# JWT 密钥（生产环境请使用强随机字符串，勿用默认值）
JWT_SECRET=your-secret-key-change-me-in-production

# JWT 过期天数
JWT_EXPIRE_DAYS=7

# 日志级别 (dev, combined, common, short, tiny)
LOG_LEVEL=dev

# 单文件上传大小限制 (MB)
MAX_UPLOAD_SIZE_MB=10
